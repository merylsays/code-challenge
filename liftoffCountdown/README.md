Given an integer **start_num**, write a code to **print** a countdown from start_num to 1. After the countdown is up, **print** the line "Liftoff!"

Example if function were called with the number 5 being passed:
```
5
4
3
2
1
Liftoff!
```
